//-  Modal
#card-display.modal.fade(tabindex='-1', ng-controller='CardsDispCtrl', role='dialog', aria-labelledby='cardDispLabel', aria-hidden='true')
  .modal-dialog.modal-lg
    .modal-content
      .modal-header
        button.close(type='button', data-dismiss='modal')
          span(aria-hidden='true') ×
          span.sr-only Close
        h4#cardDispLabel.modal-title {{title}}
      .modal-body
        | {{typeAnswer}}
        span(ng-repeat='res in responses') {{res}}
        pre {{responses | json}}
        pre {{winner | json}}
      .modal-footer
        //- button.btn.btn-default(type='button', data-dismiss='modal') Close
